All required Python libraries for SLID are included in:

requirements.txt


To install everything automatically, run:

pip install -r requirements.txt


This will install all modules used by the project without needing to install them manuall
